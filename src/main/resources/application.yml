spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio
    username: portfolio_user
    password: portfolio_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      platform: postgresql

server:
  port: 8080